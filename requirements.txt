# Dependencias do EducaflowOne - versoes fixadas.
#
# Este ficheiro e usado tanto no ambiente local quanto no build do Docker
# (ver Dockerfile). Antes, o Dockerfile descartava esta lista e reescrevia
# uma versao sem pin, entao cada deploy instalava o que fosse mais recente
# naquele dia: dois deploys do mesmo commit podiam gerar imagens diferentes.
#
# As versoes abaixo sao EXATAMENTE as que ja rodam em producao, lidas com
# `pip freeze` no container do Railway em 13/08/2026 (deploy de 19/04/2026,
# Python 3.12.13). Fixar aqui nao muda nada em producao - so torna o build
# reproduzivel. Atualizacoes de biblioteca passam a ser um passo deliberado,
# testado localmente, e nao um efeito colateral de qualquer deploy.
#
# Para atualizar: mexa numa dependencia de cada vez, teste local, e commit.
# Nao gere este ficheiro com `>` do PowerShell (grava UTF-16 e quebra o pip).

# --- diretas ---
boto3==1.42.91
dj-database-url==3.1.2
Django==6.0.4
django-anymail==15.0
django-cors-headers==4.9.0
django-storages==1.14.6
djangorestframework==3.17.1
google-auth==2.49.2
gunicorn==25.3.0
# Geracao dos relatorios .xlsx (exportacao de alunos). Python puro, sem
# compilador: o wheel e py2.py3-none-any.
openpyxl==3.1.5
pillow==12.2.0
psycopg2-binary==2.9.11
python-dotenv==1.2.2
stripe==15.0.1
whitenoise==6.12.0

# --- transitivas ---
annotated-types==0.7.0
anyio==4.13.0
asgiref==3.11.1
attrs==26.1.0
botocore==1.42.91
certifi==2026.2.25
cffi==2.0.0
charset-normalizer==3.4.7
cryptography==46.0.7
Deprecated==1.3.1
et-xmlfile==2.0.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
jmespath==1.1.0
packaging==26.1
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.2
pydantic_core==2.46.2
python-dateutil==2.9.0.post0
requests==2.33.1
s3transfer==0.16.0
six==1.17.0
sqlparse==0.5.5
standardwebhooks==1.0.1
svix==1.90.0
types-Deprecated==1.3.1.20260408
types-python-dateutil==2.9.0.20260408
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.3
wrapt==2.1.2

# Necessario apenas no Windows: o Django usa zoneinfo, que no Linux le a
# base de fusos do sistema. Producao (Linux) nao tem este pacote instalado,
# e o marcador abaixo mantem essa igualdade.
tzdata==2026.3; sys_platform == "win32"
